{
    "name": "test",
    "description": "test our custom native modules",
    "devDpendencies": {
        "lodash": "^4.17.21",
        "split2": "^4.2.0"
    },
    "scripts": {
        "test": "node install-before-test-run.js && cross-env NODE_ENV=test && cross-env DEBUG=buildkit* electron ../node_modules/mocha/bin/mocha ./*.spec.js --exit"
    },
    "type": "module"
}
